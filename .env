# Plutus Configuration
# Copy this file to .env and modify as needed

# Verbosity: 0 = silent, 1 = print every address checked
VERBOSE=0

# Substring length for memory efficiency (1-26)
# Higher values = more memory usage but fewer false positives
SUBSTRING=12

# Batch size: number of addresses to generate and check at once
# Higher values = better performance but more memory usage
BATCH_SIZE=10000

# CPU count: number of cores to use
# Set to 0 to use all available cores
CPU_COUNT=10

# Monitor interval in seconds (for monitoring service)
MONITOR_INTERVAL=60